name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4
    - name: ldid install
      run: brew install ldid
    - name: make
      run: make TARGET=ios
    - name: ls
      run: ls
    - name: zip file
      run: zip -r xxx.zip ./output/ && ls && cd ./output && ls && cd ..
    - name: git push
      run: git add -f ./output/ && git commit -m "代码提交信息"&& git push -u origin main
